import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function VenuesScreen() {
  return (
    <PlaceholderScreen
      title="Площадки"
      hint="Фаза 4: список/карта площадок, фильтр по метро, статусы «свободна/занята»."
    />
  );
}
