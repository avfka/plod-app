import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function ListScreen() {
  return (
    <PlaceholderScreen
      title="Список"
      hint="Фаза 1: лента событий с фильтрами (дата, направление, хореограф, только бесплатные)."
    />
  );
}
